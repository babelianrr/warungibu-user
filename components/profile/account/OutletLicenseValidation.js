import {Button} from '@/components/button'

export default function OutletLicenseValidation() {
  return (
    <div className="fixed inset-x-0 bottom-0">
      <section>
        <Button padding="px-4 py-2" className="text-sm flex items-center space-x-2">
          <span>Permintaan Validasi</span>
        </Button>
      </section>
    </div>
  )
}
